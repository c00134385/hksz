import 'dart:async';

// typedef OnTimeCallback = void Function();

class Task {

  // final OnTimeCallback onTimeCallback;
  //
  // // Task({required this.onTimeCallback});
  //
  // int countValue = 60;
  // Timer _timer;
  // final Duration duration = Duration(seconds: 1);
  //
  // Task(this.onTimeCallback);

  // startTimer() {
  //   countValue = 60;
  //   _timer = Timer.periodic(duration, (timer) {
  //     countValue = countValue - 1;
  //     if(this.mounted) {
  //       setState(() {
  //
  //       });
  //     }
  //
  //     if(countValue <= 0) {
  //       cancelTimer();
  //     }
  //   });
  // }
  //
  // cancelTimer() {
  //   _timer?.cancel();
  //   _timer = null;
  // }
}

